<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackduke">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ecoeats"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ecoeats"
        android:supportsRtl="true"
        android:theme="@style/Theme.HackDuke">
        <activity android:name=".LoadingActivity"></activity>
        <activity android:name=".AddFriendsActivity" />
        <activity android:name=".StartActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".FriendsActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".CameraActivity"
            android:label="@string/title_activity_camera"
            android:theme="@style/Theme.HackDuke.NoActionBar" />
    </application>

</manifest>